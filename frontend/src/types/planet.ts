export type PlanetData = {
  name: string;
  color: string;
  position: [number, number, number];
  size?: number;
  emoji?: string;
  orbitSpeed?: number;
  glowColor?: string;
  tagline?: string;
};
